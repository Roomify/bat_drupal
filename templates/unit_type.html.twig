{#
/**
 * @file unit_type.html.twig
 * Default theme implementation to present Unit type data.
 *
 * This template is used when viewing Unit type pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_unit_type()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('unit_type') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
