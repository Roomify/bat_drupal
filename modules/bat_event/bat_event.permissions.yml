administer event_type entities:
  title: 'Administer event types'
  description: 'Allows users to add event types and configure their fields.'

permission_callbacks:
  - Drupal\bat_event\EventPermissions::permissions
