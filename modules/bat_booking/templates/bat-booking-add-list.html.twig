{#
/**
 * @file
 * Default theme implementation to list booking bundles available for adding booking.
 *
 * Available variables:
 * - types: A list of unit type bundles, each with the following properties:
 *   - add_link: Link to create a piece of content of this type.
 *   - description: Description of this type of content.
 *
 * @see template_preprocess_bat_type_add_list()
 *
 * @ingroup themeable
 */
#}
{% if types is not empty %}
  <ul class="admin-list">
    {% for type in types %}
      <li class="clearfix"><span class="label">{{ type.add_link }}</span></li>
    {% endfor %}
  </ul>
{% else %}
  <p>
    {% set create_content = path('entity.bat_booking_bundle.collection') %}
    {% trans %}
      You have not created any booking bundle yet. Go to the <a href="{{ create_content }}">booking bundle creation page</a> to add a new booking bundle.
    {% endtrans %}
  </p>
{% endif %}
